#include <stdio.h>
#include "main.h"
/**
 * mul -entry point
 * @a: is an int that will be multiplied
 * @b: is an int that will be multiplied
 * Return: Always 0
 *it does the thing
 */
int mul(int a, int b)
{
return (a * b);
}
