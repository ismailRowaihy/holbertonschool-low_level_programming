#include <stdio.h>
/**
 * main -entry point
 *@argc: arg counts
 *@argv: arg array of passed words
 * Return: Always 0
 *it does the thing
 */
int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc - 1);
return (0);
}
