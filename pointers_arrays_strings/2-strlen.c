#include <stdio.h>
#include "main.h"
/**
 *_strlen -entry point
 *@s: a char pointer
 * Return: Always 0
 *it does the thing
 */
int _strlen(char *s)
{
int i;
for (i = 0; s[i]; i++)
{
}
return (i);
}
